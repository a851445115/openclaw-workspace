# 2026-03-08

- 新增 cron：`AI前沿情报简报`，每天 09:10（Asia/Shanghai）执行，抓取 X / Reddit 等来源的 AI 前沿信号，生成飞书原生文档，并将结果推送到飞书群 `oc_b82927bc13ef426d8cdc174f7d8c2fdd`。
- 已移除 `AI前沿情报简报` 任务的 `timeoutSeconds: 240` 限制，避免因抓取与写文档链路较长导致 cron 超时。
- 已手动执行一次 `AI前沿情报简报`：生成飞书文档 `https://feishu.cn/docx/H0pudctLoogfLHxAfyXcWlPEn25`，并将短消息投递到目标群 `oc_b82927bc13ef426d8cdc174f7d8c2fdd`。
- 已将 `AI前沿情报简报` 的 prompt 改为稳定抓取版：默认不用 `web_search`，改为 `browser + Google site:x.com 结果页 + Reddit 搜索页/Google site:reddit.com` 的低依赖流程，减少 X 直连与缺少搜索 API key 带来的失败概率。
- 新增 cron：`B站财经多UP共识日报`，每天 21:30（Asia/Shanghai）执行，跟踪 7 位 B 站财经 UP 主的最近视频，提炼多UP共识观点、主要分歧，并给刚步入社会年轻人的务实建议；默认推送到当前飞书群 `oc_2a08151e5102363eca1cb6533327af9c`。
- 已手动试跑一次 `B站财经多UP共识日报`：生成飞书文档 `https://feishu.cn/docx/T99CdncQ7ot2TTxl8aRcYAPdnkQ`。本轮有效覆盖 5 位 UP，主要靠 B 站上传页与部分视频页可见信息；首轮结论偏“弱共识 + 务实建议”，适合继续观察和迭代 prompt。
- 已按“更接近柏年说政经”的风格重筛 `B站财经多UP共识日报` 的名单：保留 `柏年说政经 / 巫师财经 / 陈欣教授谈财经 / 张捷财经观察` 作为高相似核心；用 `珍大户 / 战国时代_姜汁汽水` 替换掉偏消费案例或偏纪录片风格的 `财经不眠姐 / 奈斯财经`，并保留 `饭统戴老板` 作为中相似补充位。
- 已按新名单手动复跑一次，生成飞书文档 `https://feishu.cn/docx/NY6oddW0moGVdVxya3nciQCgnNe`。这版明显更聚焦宏观、政策、地缘与普通人决策，风格比上一版更接近 `柏年说政经`。
